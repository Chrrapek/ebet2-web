const styles = theme => ({
    cardList: {
        marginTop: "2em",
        display: "flex",
        flexWrap: "wrap",
        justifyContent: "center",
        width: "85%",
        textAlign: "center",
        margin: "auto",
        // boxShadow: "inset 0 0 10px #333"
    },
});

export default styles;
